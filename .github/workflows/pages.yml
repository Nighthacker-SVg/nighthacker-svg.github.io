name: Deploy static site to GitHub Pages
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare public directory
        run: |
          rm -rf public
          mkdir -p public
          # copy files into public while excluding .git and the workflow folder
          rsync -av --exclude='.git' --exclude='.github' --exclude='.gitignore' --exclude='node_modules' ./ public/

      - name: Configure Pages
        uses: actions/configure-pages@v3

      - name: Upload Pages artifact
        # Use v2 of upload-pages-artifact which no longer depends on the deprecated upload-artifact v3
        uses: actions/upload-pages-artifact@v2
        with:
          path: public

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v1
